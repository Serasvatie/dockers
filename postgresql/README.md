# Commands

Start :
```
docker compose up -d
```

Stop :
```
docker compose down -v
```

# PgAdmin

Connec to interface using the credentials inside the docker compose.
When setting up the connection to the database, use the network id : "db" and the port 5432, credentials available in the docker compose.

